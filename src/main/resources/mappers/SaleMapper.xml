<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.SaleMapper">

  <!-- sales.id を自動採番で受け取る -->
  <insert id="insertSale" 
  	parameterType="SaleCreateParam"
  	useGeneratedKeys="true" keyProperty="id">
    INSERT INTO sales (sold_at, channel_id, note)
    VALUES (#{soldAt}, #{channelId}, #{note})
  </insert>
  
  <insert id="insertSaleLine">
  	INSERT INTO sale_lines (sale_id, variant_id, qty, unit_price, cost_price)
  	VALUES (#{saleId},#{variantId},#{qty},#{unitPrice},#{costPrice})
  </insert>
  
  <select id="selectVariantPrices" resultType="map">
  	SELECT
  		price AS unitPrice,
  		cost_price AS costPrice
  	FROM item_variants
  	WHERE id = #{variantId}
  </select>
  
  <select id="selectRecentSales" resultType="SaleListRowDto">
  	SELECT
  		s.id AS id,
  		s.sold_at AS soldAt,
  		s.channel_id AS channelId,
  		s.note AS note,
  		COUNT(sl.id) AS linesCount,
  		COALESCE(SUM(sl.qty), 0) AS totalQty,
  		COALESCE(SUM(sl.qty * sl.unit_price), 0) AS totalAmount
  	FROM sales s
  	LEFT JOIN sale_lines sl ON sl.sale_id = s.id
  	GROUP BY s.id, s.sold_at, s.channel_id, s.note
  	ORDER BY s.sold_at DESC, s.id DESC
  </select>
  
  <select id="selectSaleHeader" resultType="SaleHeaderDto">
  	SELECT 
  		id,
  		sold_at AS soldAt,
  		channel_id AS channelId,
  		note
  	FROM sales
  	WHERE id = #{saleId}
  </select>
  
  <select id="selectSaleLines" resultType="SaleLineRowDto">
  	SELECT
  		sl.id AS id,
  		sl.variant_id AS variantId,
  		v.sku_code AS skuCode,
  		i.name AS itemName,
  		sl.qty AS qty,
  		sl.unit_price AS unitPrice,
  		sl.cost_price AS costPrice,
  		(sl.qty * sl.unit_price) AS lineAmount
  	FROM sale_lines sl
  	JOIN item_variants v ON v.id = sl.variant_id
  	JOIN items i ON i.id = v.item_id
  	WHERE sl.sale_id = #{saleId}
  	ORDER BY sl.id ASC
  </select>

</mapper>