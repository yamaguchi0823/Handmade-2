<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.SaleMapper">

  <!-- sales.id を自動採番で受け取る -->
  <insert id="insertSale" 
  	parameterType="SaleCreateParam"
  	useGeneratedKeys="true" keyProperty="id">
    INSERT INTO sales (sold_at, channel_id, note)
    VALUES (#{soldAt}, #{channelId}, #{note})
  </insert>
  
  <insert id="insertSaleLine">
  	INSERT INTO sale_lines (sale_id, variant_id, qty, unit_price, cost_price)
  	VALUES (#{saleId},#{variantId},#{qty},#{unitPrice},#{costPeice})
  </insert>
  
  <select id="selectVariantPrices" resultType="map">
  	SELECT
  		price AS unitPrice,
  		cost_price AS costPrice
  	FROM item_variants
  	WHERE id = #{variantId}
  </select>
  

</mapper>