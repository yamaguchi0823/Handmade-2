<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.ItemMapper">

	<select id="selectActiveItems" resultType="com.example.app.dto.ItemDto">
    SELECT
      id,
      name,
      description,
      is_active AS isActive
    FROM items
    WHERE is_active = 1
    ORDER BY id DESC
  </select>

  <select id="selectById" resultType="com.example.app.dto.ItemDto">
    SELECT
      id,
      name,
      description,
      is_active AS isActive
    FROM items
    WHERE id = #{id}
  </select>

  <insert id="insert">
    INSERT INTO items (name, description)
    VALUES (#{name}, #{description})
  </insert>

  <update id="update">
    UPDATE items
    SET
      name = #{name},
      description = #{description},
      updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

  <update id="deactivate">
    UPDATE items
    SET
      is_active = 0,
      updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>
	
</mapper>